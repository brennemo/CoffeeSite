<div class="container">
    <!-- Example row of columns -->
    <div class="row">
		<h1>My Passport</h1> 
    </div>
    <div class="row">
    	{{#each rows}}
    		<div class="col-xs-2">
    		<div class="explore-container">
	    			<a href="menu/{{this.shop_id}}">
	    				<img src="{{this.shop_photo_t}}" alt="{{this.shop_name}}" class="visited{{this.passport_visited}}"/>
	    				<h4 class="text-center">{{this.shop_name}}</h4>
	    			</a>
    			</div>
    		</div>
    		<p>{{foo}}</p>
    	{{/each}}
    </div> 
</div>

